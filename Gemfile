
source 'https://rubygems.org'

# this was intended to automate getting the latest github-pages gem, disabling due to json incompatibility w/ ruby 2.4.x
#require 'json'
#require 'open-uri'
#versions = JSON.parse(open('https://pages.github.com/versions.json').read)

gem 'github-pages', '124'
gem 'jekyll-seo-tag'

